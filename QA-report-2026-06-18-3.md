# Dugong Field Notes — QA run (2026-06-18, third pass)

**Date:** 2026-06-18 · **Scope:** blog.dugong.live (index + 15 posts + feed/sitemap/llms/robots + CNAME)
**Mode:** automated scheduled QA (autonomous; no user present)

## Result: no defects found. Blog is clean and fully wired.

A full automated sweep of every content and discovery surface passed. State is unchanged
since the day's earlier passes that shipped Dispatch Nº 62 (the-order-tag-you-set-by-hand.html)
and completed its back-link graph. No fixes were required. The standing items below are known
editorial/design calls left as-is, consistent with every prior run.

## Automated checks (all passing)

| Check | Result |
|---|---|
| sitemap.xml parses as XML | PASS |
| feed.xml parses as XML | PASS |
| feed item / guid count | 16 / 16 |
| sitemap URL / lastmod count | 16 / 16 (homepage + 15 posts) |
| JSON-LD parses | PASS on all 16 pages |
| `<div>` balance | index 155/155; all 15 posts 70/70 |
| duplicate `id` attributes | none on any page |
| dispatch numbers | unique Nº 48–62, no duplicates |
| canonical == og:url | 1 each, matched on all 16 pages |
| twitter:card / og:image:alt / twitter:image:alt | exactly 1 each on all 16 pages |
| meta description present + in range | all 16 within 100–162 chars |
| `<title>` present and unique | 16 unique / 16 pages |
| `<img>` src targets exist | all resolve |
| internal `.html` links resolve | all resolve, no broken, no self-links |
| in-page `#anchor` targets resolve | all resolve except the static featured card (standing item, below) |
| per-post self-consistency (read time vs PT#M, words vs wordCount) | PASS on all 15 posts |
| robots.txt sitemap line | present, points at sitemap.xml |
| lang + viewport on every page | present |
| feed freshness | lastBuildDate Thu, 18 Jun 2026; newest item = order-tag post (Nº 62) |
| CNAME | blog.dugong.live |

## Internal link graph

Every post links the full set of 14 siblings plus the homepage, with no self-links and zero
broken local `.html` links. Back-link graph for the newest dispatch (Nº 62) is complete: all 14
older siblings link to it. The only absolute self-URLs on each post are the `canonical` and
`og:url` tags, which are correct by design.

## Cross-surface counts (index.html)

Hero ISSUE Nº, the "dispatches published" stat, and the "browse all N dispatches" archive link
all read 62 and agree with the highest dispatch number. The decorative ticker monthly count reads
16. JSON-LD `@graph` lists 16 BlogPosting entries (lead essay + 15 posts). llms.txt references all
15 posts. feed.xml and sitemap.xml each carry 16 entries.

## Newest post self-consistency (Nº 62, the-order-tag-you-set-by-hand.html)

Ticker, visible article meta, and JSON-LD all agree: Nº 62, 9 MIN READ / `PT9M`, 1,880 WORDS /
`wordCount` 1880, dates 2026-06-18. Structured data present: BlogPosting + BreadcrumbList +
FAQPage + HowTo. `#playbook` anchor resolves.

## Style preference (no dashes)

All 8 posts created since the no-dash preference (abandoned-cart, chargeback, meta-description,
order-change, order-tag, reorder, return, sale, sold-out) plus restock carry only the 7
template-level em dashes shared across the site (title/RSS suffixes, CSS comments, ticker lines)
and zero en dashes in authored prose. Counts confirmed at em=7 / en=0 for each. Preference honored.

## Standing items (not changed, consistent with prior runs)

- Older posts that predate the no-dash preference still carry em/en dashes in authored body copy:
  bad-address 41/1, high-risk 44/1, overselling 45/1, shopify-automations 43/3, wismo 31/1, plus
  index decorative copy 29/0 (em/en). Rewriting that prose is an editorial change that risks
  altering meaning, so it stays out of scope for an automated QA fix.
- The static "DISPATCH OF THE WEEK" featured card on index.html is an `<a href="#post">` with no
  matching `#post` target. The card represents the embedded lead essay, which has no standalone
  page, so the anchor is intentionally inert placeholder copy. It has been stable across every
  prior pass; inventing a destination would be a design decision, not a regression fix, so it is
  left as-is.
- index.html uses two `<h1>` tags by design (site hero + featured lead-essay headline). Stable
  across every prior pass; left as-is.
- `HowTo` / step `ItemList` schema for the older posts' playbook sequences remains the largest
  un-captured GEO opportunity (flagged in prior SEO passes).

## Minor note (logs only, not a live-blog defect)

The blog-run-2026-06-18.md log prose refers to "the high-risk flag (Nº 53)" and "the order-change
post (Nº 51)"; the actual published pages are the reverse and internally consistent
(high-risk = Nº 51, order-change = Nº 53). The live blog is correct; only the log's prose has the
two numbers transposed. No page change needed.

## Note

Nothing was committed or deployed. This QA pass made no changes to the blog; verification only.
