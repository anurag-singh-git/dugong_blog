# QA report — 2026-07-01 (full-site sweep)

**Scope:** Whole blog — index.html + all 25 dispatch posts + sitemap.xml, feed.xml, llms.txt,
robots.txt, CNAME. Automated scheduled QA run; no user present, executed autonomously.

## Result: no defects found, site clean

No new post shipped since the last QA pass (2026-06-30, 25 posts, Nº72). The site is unchanged and
remains structurally sound. Every automated check passed. No edits were required.

## Automated checks (current state)

| Check | Result |
|---|---|
| sitemap.xml valid XML | PASS |
| feed.xml valid XML | PASS |
| sitemap loc count / feed item count | 26 / 26 (homepage + 25 posts) |
| JSON-LD parses on every post + index | PASS (no parse errors) |
| `<div>` open/close balance, all 26 HTML files | PASS (every file balanced) |
| Internal `.html` links resolve | PASS (no broken sibling links) |
| Local resource refs (png/svg/css/js) resolve | PASS (root-relative favicon/apple-touch-icon exist at repo root) |
| All 25 posts present in sitemap / feed / llms.txt / index | PASS |
| Reverse check: every sitemap loc maps to a real file | PASS (no dangling) |
| Back-link graph in-degree | uniform 24 on all 25 posts |
| Back-link graph out-degree | uniform 24 on all 25 posts |
| Dispatch numbers unique and contiguous | PASS (Nº48–Nº72, 25 unique, no gaps) |
| Placeholder/template tokens (TODO, {{}}, lorem, NaN, undefined) | none |
| Images missing alt text | none |
| Per-post visible "MIN READ" vs schema `timeRequired` | PASS (all agree) |
| Per-post canonical / og:url match filename | PASS |
| Per-post schema `dateModified` vs sitemap `<lastmod>` | PASS (0 mismatches across 25 posts) |
| robots.txt sitemap reference / CNAME | correct (blog.dugong.live) |

## Homepage counters — consistent

Hero "ISSUE Nº 72", archive "72 dispatches", and both ticker copies "24 NEW DISPATCHES THIS MONTH"
agree with the June 2026 run (Nº49 through Nº72 = 24 posts). Homepage links to all 25 posts. All
counters were already correct; no homepage edit was needed.

## Reviewed and intentionally not changed

- sitemap.xml, feed.xml, llms.txt, and index.html already carry all 25 posts with correct metadata.
  No edit required.
- Back-link graph already uniform (24/24 on all 25 posts); no card insertion needed.
- The eight older posts whose sitemap `<lastmod>` (2026-06-13) differs from their schema
  `datePublished` are correct: `lastmod` reflects the last modification (a prior back-link wiring
  pass), not the original publish date. Their schema `dateModified` matches `<lastmod>` exactly, so
  there is no inconsistency.
- Template-level em dashes shared identically across all post files (title suffix " — Dugong Field
  Notes", ticker/byline date lines, card attribution, CSS comments) left as-is for cross-page
  consistency, per every prior run. Authored body copy is dash-free in all recent posts.
- The "24 NEW DISPATCHES THIS MONTH" ticker reads for the calendar rollover to July but still shows
  June's count. This decorative marquee copy is set by the publish pipeline when a post ships; with
  no July post yet, it is left untouched, consistent with prior runs. Not a content-integrity defect.
- dateModified / sitemap lastmod / feed lastBuildDate left untouched — no content edits were made
  this run, so no freshness timestamps were bumped.

## Verdict

Site is structurally sound: all XML valid, all schema parses, all links resolve both directions, all
counts agree, back-link graph uniform, dispatch numbers unique and contiguous, all per-post metadata
self-consistent. No defects found; no changes made.

## Note

No files were changed by this QA pass — the site was already clean. No git commit or deploy was
performed, consistent with prior runs.
