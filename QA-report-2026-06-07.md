# Dugong Field Notes — QA run

**Date:** 2026-06-07 · **Scope:** blog.dugong.live (index + 5 posts + config files + icons)
**Mode:** automated scheduled QA

## Bugs found & fixed

**JSON-LD `wordCount` disagreed with the on-page word count on two posts.** The page ticker on
`the-shopify-automations-no-one-builds.html` says "2,180 WORDS" but its `BlogPosting` schema
declared `wordCount: 2100`; likewise `the-wismo-tickets-that-eat-your-afternoon.html` shows
"2,040 WORDS" but declared `wordCount: 2000`. Search and answer engines read the schema, so the
machine-readable claim contradicted the visible page. Both schemas now match the page (2180 and
2040). The other three posts already matched (1780, 1750, 1600).

## Checked — no issues

- **New favicon set (added since last run):** `favicon.svg`, `favicon.png` (32×32),
  `apple-touch-icon.png` (180×180) are all valid images, and all six pages reference them with
  correct `<link rel>` tags and root-relative paths.
- **Internal links:** every local `href` resolves; "Read next" graph complete (each post links to
  all 4 siblings, none to itself). The one flag my checker raised — `href="#post"` on the index
  featured card — is a false positive: the SPA JS intercepts `[data-nav]` clicks with
  `preventDefault()`, so the fragment never navigates.
- **Cross-page consistency:** read times, dates, dispatch numbers (Nº 47–52), and author
  attributions agree everywhere they appear — post page, homepage cards, sibling "Read next"
  cards, JSON-LD, and `llms.txt`. All five posts byline Priya Singh; the lead essay bylines
  Aanya Rao consistently (page, homepage `@graph`, `llms.txt`).
- **Dates:** visible dates, `datePublished`, `dateModified` (2026-06-07) and sitemap `lastmod`
  (2026-06-07) all consistent; the 06-07 lastmod is justified by the favicon change.
- **Metadata:** self-referential canonicals, matching `og:url`/`og:title`, `twitter:card`,
  per-page descriptions (all ≤160 chars — the previously flagged 325-char one has been
  tightened), `robots` meta is index,follow on every page (no stray noindex).
- **Structured data:** all JSON-LD parses; homepage `@graph` lists the lead essay plus all 5
  posts with correct authors/dates; breadcrumbs, FAQ (3 relevant Q&As per post), Person/
  Organization blocks intact.
- **Crawler config:** `sitemap.xml` lists exactly the 6 live URLs; `robots.txt` allows search +
  AI crawlers and points at the sitemap; `CNAME` matches the canonical domain; `og-image.png`
  (1200×630) present.
- **Statistics:** headline numbers quoted in `llms.txt`, homepage card art, and meta
  descriptions ($443B / $48B / $191 / 0.65% / 18% / 14% / 1,374 / 5–10% / 40%) all appear in
  the corresponding article bodies.
- **HTML structure:** all six files parse with balanced tags.

## Recommendations (not auto-changed)

- The homepage hero stat and archive link both say **"48 dispatches published"**, but the newest
  dispatch is Nº 52. The copy already said 48 back when the latest issue was Nº 47, so the
  intended mapping between issue numbers and the count is unclear — flagging rather than
  guessing. If dispatch count = latest issue number, both spots should read 52.
- Visible word-count tickers overstate the measured body text by roughly 25–30% on most posts
  (e.g. wismo claims 2,040 words, the rendered article body is ~1,400). Internally consistent
  now, but worth a recount if accuracy matters.

## Note

Two one-line edits to the working tree; no git commit or deploy was performed.
