# Dugong Field Notes — QA run (2026-06-14)

**Date:** 2026-06-14 · **Scope:** blog.dugong.live (index + 11 posts + feed/sitemap/llms/robots + icons)
**Mode:** automated scheduled QA (autonomous; no user present)

## Result: clean. No new defects found.

Today's daily blog run published Dispatch Nº 58 ("The abandoned cart that gets one email")
and the same-day SEO/GEO run already caught and fixed the two defects that pattern always
leaves behind: an over-length meta description on the new post and an incomplete back-link
graph. This QA pass independently re-swept the whole site in the settled state and confirms
every surface is correct. Nothing required fixing.

## Verified — no issues

- **HTML structure.** `<div>` open/close balanced on all 12 HTML files (index 143/143, every
  post 58/58). No duplicate `id` attributes anywhere. Title, `lang`, charset, and viewport
  present on every page.
- **Structured data.** Every `application/ld+json` block parses as valid JSON (one block per
  page). Homepage `Blog` node lists 12 `blogPost` entries (inline lead essay + 11 post files),
  newest is the abandoned-cart post.
- **Dispatch numbering.** The 11 post files carry unique numbers Nº 48 through Nº 58 in date
  order, no duplicates. Hero reads **ISSUE Nº 58**, the stat reads **58 dispatches published**,
  and the archive link reads **browse all 58 dispatches** — all three agree with the highest
  dispatch number.
- **Internal link graph.** Every page links to all 10 sibling posts plus the homepage (11
  unique local targets each), no self-links, and zero broken local `.html` links across all
  pages. The new Nº 58 back-link is present on every older post exactly once.
- **Canonical / Open Graph / Twitter.** `rel="canonical"` equals `og:url` on all 12 pages
  (one each). `og:image:alt` and `twitter:image:alt` present exactly once per page; no
  anomalies in the image-alt or twitter:card scan.
- **Meta descriptions.** All within SERP limits: index 100 chars, all 11 posts in the 146–160
  range. The new post is 160 (already trimmed from 192 by today's SEO run).
- **Freshness metadata.** Each post carries one `article:published_time` and one
  `article:modified_time`; OG dates equal the matching schema `datePublished` / `dateModified`
  on every post. Per-URL `sitemap.xml` `<lastmod>` matches each post's `dateModified` with
  zero mismatches (new post 2026-06-14, homepage 2026-06-14, the rest 2026-06-13).
- **Feed / sitemap / llms.txt.** Both XML files are well-formed. Sitemap lists 12 URLs
  (homepage + 11 posts), feed lists 12 items (guid count 12), and llms.txt references all 11
  posts. No post missing from any of the three. Feed `lastBuildDate` is Sun, 14 Jun 2026 and
  the newest item is the abandoned-cart post.
- **New post self-consistency.** Nº 58 ticker, visible meta, and schema all agree: Nº 58,
  JUN 14 2026, 9 MIN READ / `PT9M`, 1,880 words visible / `wordCount` 1880, dates 2026-06-14,
  author Priya Singh.
- **Crawler config / assets.** `robots.txt` allows all major search and AI crawlers and points
  at the sitemap; `CNAME` is `blog.dugong.live`; every referenced icon and asset
  (`favicon.svg/.png`, `apple-touch-icon.png`, `og-image.png`) exists on disk.

## Recommendations (not auto-changed, consistent with prior runs)

- The homepage hero still reads **"13 NEW DISPATCHES THIS MONTH"** — decorative copy not tied
  to a countable source. Editorial call; left as-is.
- The older posts (Nº 48–55, plus wismo) still carry many em dashes in their authored bodies,
  predating the no-dash style preference. The new posts (Nº 56, 57, 58) and every card/meta
  edited by the daily runs comply (template-level dashes only). Rewriting the older authored
  prose is an editorial change that risks altering meaning, not a QA fix, so it is left as-is.
- Carried over: folding the meta-length check and full back-link insertion into the blog-run
  step itself would remove the recurring next-day cleanup. `HowTo` / step `ItemList` schema for
  the ordered "AI playbook" sequences remains the largest un-captured GEO opportunity.

## Note

No changes were made this run — the site was already correct. Consistent with prior runs,
nothing was committed or deployed.
