# Dugong Field Notes — QA run (second run of 2026-06-13)

**Date:** 2026-06-13 · **Scope:** blog.dugong.live (index + 10 posts + feed/sitemap/llms/robots + icons)
**Mode:** automated scheduled QA (autonomous; no user present)

## Result: two issues found and fixed

An earlier run today published Dispatch Nº 56, and a second blog run added Dispatch Nº 57
("The sold-out products that sink your collection"). The first QA pass today covered the state
through Nº 56. This pass swept the settled state with Nº 57 in place and found two real defects
from the Nº 57 publish, both now fixed. Everything else passed.

## Fixed

- **Meta description too long on the new Nº 57 post.**
  `the-sold-out-products-that-sink-your-collection.html` had a 203-character
  `<meta name="description">`, well over the ~160-char SERP limit every other page respects.
  Trimmed to 146 chars without losing meaning: "Shopify can't reliably push sold-out products to
  the bottom of a collection, so dead tiles greet your best traffic. The AI playbook that fixes it."
  No em/en dashes introduced. All eleven meta descriptions are now in the 100–160 range.

- **Seven older posts were missing the Read-next back-link to Nº 57.** The second blog run wired
  Nº 57 into the feed, sitemap, llms.txt, homepage card/JSON-LD, and into the Read-next blocks of
  only two siblings (Nº 50 overselling and Nº 56 restock). The other seven posts (Nº 48, 49, 51,
  52, 53, 54, 55) still had eight-card Read-next blocks and did not link to the new post. Inserted
  the standard sold-out card (matching the markup already used on the restock post) at the top of
  each of those seven Read-next grids. Every post now links to all nine siblings. Div balance is
  preserved on each edited file (52 to 55, the +3 being the card's three nested divs) and all files
  still parse clean.

## Verified — no issues

- **Dispatch numbering and counts agree.** The ten post files map cleanly to Nº 48–57 in date
  order (inline lead essay at Nº 47). Hero badge reads **ISSUE Nº 57** and the archive footer reads
  **browse all 57 dispatches** — both match the highest dispatch number. No duplicate dispatch
  numbers across posts.
- **Read-next internal linking is complete.** Every post's Read-next now contains all nine sibling
  cards, the new Nº 57 included. Zero broken local `.html` links across all eleven pages; no post
  links to itself.
- **Canonical / Open Graph / Twitter:** `rel="canonical"` equals `og:url` on all eleven pages,
  exactly one each. `twitter:card` is `summary_large_image` everywhere; `og:image:alt` and
  `twitter:image:alt` present exactly once per page.
- **Freshness metadata consistent.** Each post carries one `article:published_time` and one
  `article:modified_time`; OG published equals schema `datePublished` and OG modified equals schema
  `dateModified` on every post. `dateModified` is 2026-06-13 across all posts, matching every
  `sitemap.xml` lastmod and the feed `lastBuildDate` (Sat, 13 Jun 2026).
- **Structured data:** every JSON-LD block on all eleven pages parses as valid JSON. Homepage `Blog`
  node lists 11 `BlogPosting` entries (10 post files + inline lead essay).
- **Feed / sitemap / llms.txt:** all three list the same 10 post URLs and include Nº 57; both XML
  files are well-formed. No post missing from any of the three.
- **HTML structure:** `<div>` open/close balanced on every file; no duplicate `id` attributes;
  titles present and unique on all eleven pages. `lang`, charset, and viewport present everywhere.
  All eleven HTML files and both XML files parse with zero errors.
- **Crawler config / assets:** `robots.txt`, `CNAME` (`blog.dugong.live`), and all referenced icons
  (`favicon.svg/.png`, `apple-touch-icon.png`, `og-image.png`) intact and consistent with canonicals.

## Recommendations (not auto-changed, consistent with prior runs)

- Homepage hero still reads **"13 NEW DISPATCHES THIS MONTH"** — decorative copy not tied to a
  countable source. Editorial call; left as-is.
- Older posts (Nº 48–55) still carry many em dashes in their authored bodies, predating the no-dash
  style preference. Rewriting them is an editorial change, not a QA fix, so left as-is; the new
  Nº 57 post already complies in its authored body and the cards/meta I edited introduce no dashes.

## Note

Two bugs fixed: the Nº 57 over-length meta description and the seven missing Read-next back-links to
Nº 57. All changes are staged in the working tree only; no git commit or deploy was performed,
consistent with prior runs.
