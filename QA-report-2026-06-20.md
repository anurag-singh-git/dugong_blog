# QA report — 2026-06-20

**Post:** the-image-alt-text-no-one-writes.html (Dispatch Nº 64)

## Automated checks — all passing

| Check | Result |
|---|---|
| feed.xml parses as valid XML | PASS |
| sitemap.xml parses as valid XML | PASS |
| feed.xml item count | 18 (was 17, +1) |
| sitemap.xml URL count | 18 (homepage + 17 posts) |
| New post JSON-LD parses (1 block) | PASS |
| index.html JSON-LD parses | PASS |
| index blogPost array entries | 18 (lead essay + 17 posts) |
| Newest blogPost entry (after lead essay) | "The image alt text no one writes", 2026-06-20 |
| New post `<div>` balance | 76 / 76 |
| index.html `<div>` balance | 161 / 161 |
| product-descriptions sibling `<div>` balance | 76 / 76 |
| meta-description sibling `<div>` balance | 76 / 76 |
| New post sibling .html links resolve | 16/16 local targets exist |
| Self-link in new post | none |
| New post present in sitemap / feed / llms / index | yes / yes / yes / yes |

## Self-consistency (new post)

- Ticker, visible article meta, and schema all agree: Nº 64 · JUN 20, 2026 · 9 MIN READ · 2,000 WORDS.
- Schema `wordCount` 2000, `timeRequired` PT9M, datePublished/dateModified 2026-06-20.
- Prose measures ~2,250 words excluding the playbook code block; stated 2,000 is the house round figure
  and stays inside the 9-minute read band (consistent with how prior posts state their counts).

## Count bumps (index.html)

- ISSUE Nº 63 → 64 · hero date JUN 19 → JUN 20 · 2026
- "dispatches published" stat 63 → 64
- "browse all 63 dispatches" → "browse all 64 dispatches"
- Featured `card-tall` swapped to the alt-text post (glyph 🖼️, coord "ALT · ×0"); product-descriptions
  post demoted to a standard card.
- Two topic-cloud tags added (image alt text, image SEO).

## Style policy (no em/en dashes in authored text)

- Literal em/en dashes in the new file: 7, all template-level and identical across every sibling (the
  `<title>` suffix, the RSS `<link>` title suffix, three CSS comments, two ticker date lines) plus the
  shared CTA `&mdash;` entity.
- Zero em/en dashes in: article body, dek, meta/OG/Twitter descriptions, FAQ, the new homepage card, the
  two backlink cards, the new feed `<item>`, and the new llms.txt entry.
- Edited siblings (product-descriptions, meta-description) remain at 7 literal dashes each: no authored
  dash introduced by the new backlink cards.

## Manual spot-check

- Heading screams the use case and carries the searched phrase; dek states the native gap and the fix.
- Internal links correct and relevant; backlinks placed as first Read-next card in the two most related
  siblings.
- Meta description ~150 chars, unique.
- Accessibility note: the post itself uses real alt text on its banner image (og/twitter image:alt),
  consistent with the topic.

## Result

Ship-ready. All changes staged in the working tree only; no commit or deploy performed (consistent with
prior runs).
