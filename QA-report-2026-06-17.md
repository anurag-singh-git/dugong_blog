# Dugong Field Notes — QA run (2026-06-17)

**Date:** 2026-06-17 · **Scope:** blog.dugong.live (index + 13 posts + feed/sitemap/llms/robots + icons)
**Mode:** automated scheduled QA (autonomous; no user present)

## Result: no defects found. Blog is clean and fully wired.

A full automated sweep of every content and discovery surface passed. No new posts have
shipped since the Nº 60 sale post (2026-06-16), so the link graph and counts are unchanged
from the prior clean pass. No fixes were required. The standing items below are known
editorial calls left as-is, consistent with every prior run.

## Automated checks (all passing)

| Check | Result |
|---|---|
| sitemap.xml parses as XML | PASS |
| feed.xml parses as XML | PASS |
| feed item / guid count | 14 / 14 (newest = sale post, Nº 60) |
| sitemap URL / lastmod count | 14 / 14 (homepage + 13 posts) |
| JSON-LD parses | PASS, 1 block on each of the 14 pages |
| `<div>` balance | index 149/149; all 13 posts 64/64 |
| duplicate `id` attributes | none on any page |
| dispatch numbers | unique Nº 48–60, no duplicates |
| canonical == og:url | 1 each, matched on all 14 pages |
| twitter:card / og:image:alt / twitter:image:alt | exactly 1 each on all 14 pages |
| meta description present + in range | all 14 within SERP limit (~100–160) |
| `<title>` present and unique | all 14, no duplicates |
| `<img>` src targets exist | all resolve |
| internal `.html` links resolve | all resolve, no broken, no self-links |
| robots.txt sitemap line | present, points at sitemap.xml |
| lang + viewport on every page | present |
| CNAME | blog.dugong.live |

## Internal link graph

Every post links the full set of 12 siblings plus the homepage, with no self-links and zero
broken local `.html` links. (The only absolute self-URLs on each post are the `canonical`
and `og:url` tags, which are correct by design.) Back-link graph for the newest dispatch
(Nº 60) is complete: all 12 older siblings link to it.

## Newest post self-consistency (Nº 60, the-sale-you-start-by-hand.html)

Ticker, visible article meta, and JSON-LD all agree: Nº 60, 9 MIN READ / `PT9M`,
1,870 WORDS / `wordCount` 1870, `datePublished` and `dateModified` 2026-06-16, OG
published/modified times 2026-06-16. Feed `lastBuildDate` is Tue, 16 Jun 2026 and the
newest feed item is the sale post.

## Cross-surface counts

Homepage hero ISSUE Nº and the "browse all N dispatches" archive link both read 60 and
agree with the highest dispatch number. JSON-LD `@graph` on the index lists 14 BlogPosting
entries (lead essay + 13 posts). llms.txt references all 13 posts.

## Style preference (no dashes)

Every post created since the no-dash preference (abandoned-cart, chargeback, order-change,
reorder, restock, return, sale, sold-out) carries only the 7 template-level em dashes shared
across the whole site (title/RSS suffixes, CSS comments, ticker lines) and zero en dashes in
authored prose — preference honored. Counts confirmed at em=7 / en=0 for each.

## Standing items (not changed, consistent with prior runs)

- Older posts that predate the no-dash preference still carry em/en dashes in authored body
  copy: bad-address 41/1, high-risk 43/1, overselling 45/1, shopify-automations 43/3,
  wismo 31/1, plus index decorative copy 29/0 (em/en). Rewriting that prose is an editorial
  change that risks altering meaning, so it stays out of scope for an automated QA fix.
- index.html uses two `<h1>` tags by design: the site hero ("Workflows, thought through.")
  and the featured lead-essay headline ("The death of the drag-and-drop builder."). This has
  been stable across every prior pass; changing the heading hierarchy is a design decision,
  not a regression, so it is left as-is.
- The static "DISPATCH OF THE WEEK" featured card and the hero "NEW DISPATCHES THIS MONTH"
  decorative ticker are not tied to a countable source; left as editorial copy.
- `HowTo` / step `ItemList` schema for the ordered playbook sequences remains the largest
  un-captured GEO opportunity (flagged in prior SEO passes).

## Note

Nothing was committed or deployed. This QA pass made no changes to the blog; verification only.
