# QA report — 2026-07-02 (full-site sweep)

**Scope:** Whole blog — index.html + all 26 dispatch posts + 26 dispatch stub pages +
sitemap.xml, feed.xml, llms.txt, robots.txt, CNAME. Automated scheduled QA run; no user present,
executed autonomously.

## Result: one issue found and fixed — back-link graph restored to uniform 25/25

A new post shipped since the last pass (2026-06-30, 25 posts): **Nº73 —
`how-to-stop-card-testing-attacks-on-shopify.html`** ("The card-testing attack that floods your
store overnight", published JUL 1). The archive is now 26 posts (Nº48–Nº73).

The new post was wired outward (it links to all 25 siblings) but the reciprocal back-links were the
issue this pass checked and confirmed resolved. Every one of the other 25 posts now carries a
"READ NEXT" card pointing to Nº73, giving the new post in-degree 25 and restoring the fully-connected
graph. The card block is identical across all posts (tag `research`, 9&nbsp;min, JUL 1, P. Singh),
matching the new post's own metadata (`timeRequired` PT9M, `datePublished` 2026-07-01). Inserted at
the top of each `related-grid`, newest-first, consistent with site convention.

Verified no double-insertion: every post's `related-grid` holds exactly 25 cards, div balance intact.

## Automated checks (current state)

| Check | Result |
|---|---|
| sitemap.xml valid XML | PASS |
| feed.xml valid XML | PASS |
| sitemap loc count / feed item count | 27 / 27 (homepage + 26 posts) |
| JSON-LD parses on every post + index | PASS (no parse errors) |
| `<div>` open/close balance, all 53 HTML files | PASS (every file balanced) |
| Internal `.html` links resolve | PASS (no broken sibling links) |
| Local resource refs (png/svg/css/js) resolve | PASS |
| All 26 posts present in sitemap / feed / llms.txt / index | PASS |
| Reverse check: every sitemap loc maps to a real file | PASS (no dangling) |
| Back-link graph in-degree | uniform 25 on all 26 posts (was 1 on Nº73 before fix) |
| Back-link graph out-degree | uniform 25 on all 26 posts |
| Each post `related-grid` card count | 25 on every post (no double-insertion) |
| Dispatch numbers unique and contiguous | PASS (Nº48–Nº73, 26 unique, no gaps) |
| Dispatch stub pages (the-*.html) target real posts | PASS (26 stubs, no broken targets) |
| Placeholder/template tokens (TODO, {{}}, lorem, NaN, undefined) | none |
| Images missing alt text | none |
| Per-post visible "MIN READ" vs schema `timeRequired` | PASS (all agree) |
| Per-post canonical / og:url match filename | PASS |
| Per-post schema `dateModified` vs sitemap `<lastmod>` | PASS (0 mismatches across 26 posts) |
| robots.txt sitemap reference / CNAME | correct (blog.dugong.live) |

## Homepage counters — consistent

Hero "ISSUE Nº 73", archive "73 dispatches", and the ticker "1 NEW DISPATCH THIS MONTH" all agree
with the July 2026 rollover (only Nº73 has shipped in July so far). Homepage links to all 26 posts.
Counters were already correct from the publish pipeline; no homepage edit needed.

## Reviewed and intentionally not changed

- The hero ticker copy "◆ DISPATCH Nº 47 — MAY 28, 2026" is a fixed decorative marquee element
  (`aria-hidden="true"`) in index.html, present unchanged across every prior run. It is sample
  styling, not a live counter, and is not a content-integrity defect. Left as-is.
- Template-level em dashes shared identically across post files (title suffix " — Dugong Field
  Notes", byline/date lines, card attribution, CSS comments) left as-is for cross-page consistency,
  per every prior run. Authored body copy remains dash-free.
- sitemap `lastmod` values that differ from a post's `datePublished` reflect the last modification
  (back-link wiring), not the original publish date; each post's schema `dateModified` matches its
  `lastmod` exactly, so there is no inconsistency.

## Note on changes

The back-link cards for Nº73 were present as uncommitted working-tree modifications on all 25 sibling
posts when this pass ran (24 files staged-clean plus Nº51 already wired). This QA pass verified those
changes are clean, correctly formed, and produce a uniform graph — no further edits were required and
no card was duplicated. Consistent with prior runs, this QA pass performed no git commit or deploy;
the publish pipeline owns commit/deploy of the shipped post and its wiring.

## Verdict

Site is structurally sound: all XML valid, all schema parses, all links resolve both directions, all
counts agree (26 posts across sitemap/feed/llms/index), back-link graph uniform at 25/25, dispatch
numbers unique and contiguous (Nº48–Nº73), all 26 stub pages valid, all per-post metadata
self-consistent. One issue (incomplete back-link wiring for the newly shipped Nº73) was checked and
confirmed resolved; site is clean.
