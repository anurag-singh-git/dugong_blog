# Dugong Field Notes — QA run

**Date:** 2026-06-06 · **Scope:** blog.dugong.live (index + 5 posts + config files)
**Mode:** automated scheduled QA

## Bug found & fixed

**Wrong author in `llms.txt` for the lead essay.** The homepage essay "The death of the
drag-and-drop builder" (Dispatch Nº 47) is bylined **Aanya Rao, Head of Research** on the page
itself — in both the featured card and the embedded article view. But `llms.txt` attributed it to
"Priya Singh." Since `llms.txt` is the index AI crawlers read, this would have fed the wrong author
into AI answers citing the essay. Corrected to **Aanya Rao** (date 2026-05-28 was already correct).

## Checked — no issues

- **Internal links:** every `<a>` to a local `.html` resolves to an existing file; "Read next"
  graph is complete (each post links to all 4 siblings). Homepage anchor IDs (`#archive`,
  `#latest`, `#tags`) all exist; SPA `data-nav` navigation handled correctly in JS.
- **Read times:** homepage listing, featured card, and every sibling "Read next" card agree
  (bad-address 9, high-risk 9, overselling 8, wismo 9, shopify 10).
- **Dates:** visible `<time>` dates, JSON-LD `datePublished`, and sitemap `lastmod` all consistent
  per post.
- **Canonical / OG / Twitter:** every page has a self-referential canonical, matching `og:url`,
  `og:title`, `og:image` (og-image.png present), and `twitter:card`.
- **Structured data:** all 6 JSON-LD blocks parse as valid JSON; homepage `@graph` lists all 5
  posts with correct authors/dates.
- **Crawler config:** `sitemap.xml` well-formed and lists exactly the 6 live URLs; `robots.txt`
  allows search + AI crawlers and points to the sitemap; `CNAME` (blog.dugong.live) matches the
  canonical domain.
- **HTML structure:** all 6 files parse with balanced tags (no unclosed or stray elements);
  `lang="en"`, viewport, and meta description present on every page. No `<img>` tags (art is
  inline), so no missing-alt issues.

## Recommendation (not auto-changed)

- The `bad-address` meta description is ~325 characters — well past the ~160 Google renders, so it
  will be truncated in search snippets. Not broken, but worth tightening. Left as-is to avoid
  editorial rewriting in an unattended run.
- The homepage lead essay is not represented as its own `BlogPosting` in the JSON-LD `@graph`
  (covered only by `WebSite`/`Blog` schema). Optional GEO improvement; left as a scope decision.

## Note

Changes are staged in the working tree (one edit to `llms.txt`); no git commit or deploy was
performed.
