# QA report — 2026-06-21

**Scope:** blog.dugong.live — index + 17 posts + feed/sitemap/llms/robots/CNAME
**Mode:** automated scheduled QA (autonomous, no user present)

## Result: clean pass — no defects found, no changes made

A full automated sweep ran clean on every content and discovery surface. No correctness
defects were found, so no files were modified this run. Details below.

## Automated checks (all passing)

| Check | Result |
|---|---|
| HTML parses, tags balanced (strict parser, all 18 pages) | PASS — no unclosed or stray tags |
| `<div>` open/close balance | balanced on every file |
| duplicate `id` attributes | none |
| `<title>` present and unique | 18 / 18 |
| canonical == og:url | matched, exactly 1 each on all 17 posts |
| twitter:card / og:image:alt | exactly 1 each on every post |
| meta description length | all 17 posts within 120–170 chars |
| JSON-LD parses | PASS on all 18 pages |
| `<img>` alt attributes | every image has alt text |
| lang + viewport | present on every page |
| templating artifacts (undefined / NaN / {{ }} / TODO / lorem) | none |
| internal `.html` links resolve | all resolve, no broken, no self-links |
| in-degree complete graph | 16 / 16 on every post (distribution = {16}) |
| related-grid cards | no self-cards, no duplicate cards in any grid |
| dateModified >= datePublished | holds on every post |
| feed.xml / sitemap.xml parse as XML | PASS |
| feed item / guid count | 18 / 18, no duplicate guid |
| feed pubDate ordering / lastBuildDate | descending; Sat, 20 Jun 2026; newest = image-alt |
| sitemap loc count | 18 (homepage + 17 posts), unique, no dangling |
| llms.txt post references | 17 / 17 |
| robots.txt sitemap line | present, points at sitemap.xml |
| CNAME | blog.dugong.live |

## Cross-surface counts (index.html)

Hero ISSUE Nº (64), hero date JUN 20 · 2026, and the "browse all 64" archive link all agree
with the highest dispatch number and with the JSON-LD array (18 BlogPosting entries = lead
essay + 17 posts). Newest post across feed, sitemap, and the index list is the image-alt post.
The "Nº 47" references on the index are the pinned lead essay (DISPATCH Nº 47, May 28), which
is the featured headline by design and not a count discrepancy.

## Items reviewed and intentionally not changed

- **wordCount metadata** is author-declared and approximate. A rough body-text recount differs
  from the declared value on several posts in both directions (the schema.org field is an
  estimate, not an exact count). This matches the prior baseline, so the values were left as-is
  rather than overwriting metadata from an unreliable automated count.
- **Em/en dashes in older body copy** (bad-address, high-risk, overselling, shopify-automations,
  wismo) still predate the no-dash style preference. Rewriting that prose is an editorial change
  that risks altering meaning, so it stays out of scope for an automated QA fix, consistent with
  every prior run.
- index.html uses two top-level headings by design (site hero + featured lead-essay headline).
- HowTo / step ItemList schema for older posts' playbook sequences remains an open GEO
  opportunity flagged in prior SEO passes, not a correctness defect.

## Note

No files changed this run. No git commit or deploy performed, consistent with prior runs.
