# QA report — 2026-06-20 (run 2)

**Scope:** blog.dugong.live — index + 17 posts + feed/sitemap/llms/robots/CNAME
**Mode:** automated scheduled QA (autonomous, no user present)

## Result: one defect found and fixed — internal back-link graph completed

A full automated sweep ran clean on every content and discovery surface except the internal
link graph. The two most recent posts were under-linked relative to the site's documented
standard (a complete graph where every post links to every sibling, confirmed in the
2026-06-19 QA pass). This run restored the complete graph. Details below.

## Defect: incomplete back-link graph (fixed)

The daily blog-run that shipped Nº 64 (the image-alt post) added back-links from only the
two most related siblings and left "back-link from every older post" as a deferred SEO pass.
That left two posts under-discoverable:

| Post | In-degree before | In-degree after |
|---|---|---|
| the-image-alt-text-no-one-writes.html (Nº 64) | 2 | 16 |
| the-product-descriptions-youll-never-finish-writing.html (Nº 63) | 3 | 16 |

Every other post already sat at the full in-degree of 16. The fix prepended the two missing
"Read next" cards (newest first, matching the site convention) to the related-grid of each
post that lacked them: 14 files touched, +2 cards on thirteen, +1 on the
shopify-automations post (which already linked product-descriptions). The card markup was
copied verbatim from the canonical cards already present on sibling pages, so tag, read time,
title, dek, date, and author all match.

After the fix: every post links exactly 16 unique siblings and has in-degree 16 — a complete
graph with no self-cards and no duplicate cards in any grid.

## Automated checks (all passing)

| Check | Result |
|---|---|
| feed.xml / sitemap.xml parse as XML | PASS |
| feed item / guid count | 18 / 18, no duplicate guid |
| sitemap loc count | 18 (homepage + 17 posts), no dangling, no duplicate loc |
| feed pubDate ordering / lastBuildDate | descending; Sat, 20 Jun 2026; newest = image-alt |
| JSON-LD parses | PASS on all 18 pages |
| index blogPost array | 18 entries (lead essay + 17 posts); newest after essay = image-alt |
| `<div>` balance (all pages, post-edit) | balanced on every file |
| duplicate `id` attributes | none |
| `<title>` present and unique | 18 / 18 |
| canonical == og:url | matched, 1 each on all 17 posts |
| twitter:card / og:image:alt / twitter:image:alt | exactly 1 each on every page |
| meta description length | all 17 posts within 146–162 chars |
| read time vs `timeRequired`, words vs `wordCount` | consistent on all 17 posts |
| internal `.html` links resolve | all resolve, no broken, no self-links |
| in-degree complete graph (post-fix) | 16 / 16 on every post |
| llms.txt post references | 17 / 17 |
| index links to every post | yes |
| lang + viewport on every page | present |
| templating artifacts (undefined / NaN / {{ }} / TODO / lorem) | none |
| robots.txt sitemap line | present, points at sitemap.xml |
| CNAME | blog.dugong.live |

## Cross-surface counts (index.html)

Hero ISSUE Nº (64), the "64" dispatches-published stat, and the "browse all 64 / all 64
dispatches" archive links all agree with the highest dispatch number and with the JSON-LD
array (18 BlogPosting entries = lead essay + 17 posts). Hero date JUN 20 · 2026.

## Style preference (no dashes)

The inserted cards introduced zero em/en dashes. Edited posts remain at the standard 7
template-level em dashes (title/RSS suffixes, CSS comments, ticker lines) and 0 en dashes,
identical to every sibling.

## Standing items (unchanged, consistent with prior runs)

- Older posts (bad-address, high-risk, overselling, shopify-automations, wismo) still carry
  em/en dashes in authored body copy that predates the no-dash preference. Rewriting that
  prose is an editorial change out of scope for an automated QA fix.
- index.html uses two top-level headings by design (site hero + featured lead-essay headline).
- HowTo / step ItemList schema for older posts' playbook sequences remains the largest
  un-captured GEO opportunity (flagged in prior SEO passes), not a correctness defect.

## Note

Changes staged in the working tree only; no git commit or deploy performed, consistent with
prior runs. Files changed by this pass: the 14 post HTML files listed above. (feed.xml,
index.html, llms.txt, sitemap.xml carried pre-existing edits from the prior blog-run,
untouched by this QA pass.)
