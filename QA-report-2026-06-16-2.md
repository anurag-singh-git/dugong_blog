# Dugong Field Notes — QA run (2026-06-16, second pass)

**Date:** 2026-06-16 · **Scope:** blog.dugong.live (index + 13 posts + feed/sitemap/llms/robots + icons)
**Mode:** automated scheduled QA (autonomous; no user present)

## Result: no defects found. Blog is clean and fully wired.

A full automated sweep of every discovery and content surface passed. The recurring back-link
gap (each new dispatch shipping with only the two most-related siblings linking back) was already
closed for Nº 60 earlier today, so every one of the 13 posts now links the full sibling set. No
fixes were required this pass; the standing items below are known editorial calls left as-is,
consistent with prior runs.

## Automated checks (all passing)

| Check | Result |
|---|---|
| sitemap.xml parses as XML | PASS |
| feed.xml parses as XML | PASS |
| feed item / guid count | 14 / 14 (newest = sale post, Nº 60) |
| sitemap URL / loc count | 14 / 14 (homepage + 13 posts) |
| JSON-LD parses | PASS, 1 block on each of the 14 pages |
| `<div>` balance | index 149/149; all 13 posts 64/64 |
| duplicate `id` attributes | none on any page |
| dispatch numbers | unique Nº 48–60, no duplicates |
| canonical == og:url | 1 each, matched on all 14 pages |
| twitter:card / og:image:alt / twitter:image:alt | exactly 1 each on all 14 pages |
| meta description length | index 100; posts 146–160 (all within SERP limit) |
| `<img>` src targets exist | all resolve |
| index.html local links resolve | all resolve, no broken |
| referenced assets exist | favicon.svg/.png, apple-touch-icon.png, og-image.png |
| robots.txt sitemap line | present, points at sitemap.xml |
| CNAME | blog.dugong.live |

## Internal link graph

Every post links all 12 unique siblings (13 local targets including the homepage), with no
self-links and zero broken local `.html` links. All 12 older siblings back-link to Nº 60 (the
sale post) — the back-link graph for the newest dispatch is complete. Read-next card titles were
spot-checked against their href targets on the new post; no title/target mismatches.

## New post self-consistency (Nº 60, the-sale-you-start-by-hand.html)

Ticker, visible article meta, and schema all agree: Nº 60, JUN 16 2026, 9 MIN READ / `PT9M`,
1,870 WORDS / `wordCount` 1870, `datePublished` and `dateModified` 2026-06-16, OG published/
modified times 2026-06-16, author Priya Singh. Sitemap `<lastmod>` for the post and the homepage
are both 2026-06-16; feed `lastBuildDate` is Tue, 16 Jun 2026 and the newest item is the sale post.

## Cross-surface counts

Homepage hero ISSUE Nº, the "dispatches published" stat, and the "browse all N dispatches" archive
link all read 60 and agree with the highest dispatch number. JSON-LD `blogPost` array lists 14
entries (lead essay + 13 posts). llms.txt references all 13 posts.

## Style preference (no dashes)

The newest dispatch and every post created since the no-dash preference (abandoned-cart,
chargeback, order-change, reorder, return, sale, sold-out) carry only the 7 template-level em
dashes shared across the site (title/RSS suffixes, three CSS comments, two ticker lines) and zero
en dashes in authored prose — preference honored.

## Standing items (not changed, consistent with prior runs)

- Older posts (Nº 48–54 group plus wismo, and decorative copy in index.html) still contain
  em/en dashes in authored bodies that predate the no-dash preference. Rewriting that prose is an
  editorial change that risks altering meaning, so it is out of scope for an automated QA fix.
  Counts unchanged this pass: bad-address 41/1, high-risk 43/1, overselling 45/1,
  shopify-automations 43/3, wismo 31/1, index 29/0 (em/en).
- The static "DISPATCH OF THE WEEK" featured card and the hero "NEW DISPATCHES THIS MONTH"
  decorative ticker (now 14) are not tied to a countable source; left as editorial copy.
- `HowTo` / step `ItemList` schema for the ordered playbook sequences remains the largest
  un-captured GEO opportunity (flagged in prior SEO passes).

## Note

Nothing was committed or deployed; the working tree already held today's blog-run and back-link
edits, and this QA pass made no further changes. Verification only.
