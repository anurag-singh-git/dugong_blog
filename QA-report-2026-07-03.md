# QA report — 2026-07-03 (full-site sweep)

**Scope:** Whole blog — index.html + 27 dispatch posts + 27 dispatch stub pages +
sitemap.xml, feed.xml, llms.txt, robots.txt, CNAME. Automated scheduled QA run; no user present,
executed autonomously.

## Result: no issues found. Site is clean.

No new post shipped since the last pass (2026-07-02, Nº74). The archive is unchanged at 27 posts
(Nº48–Nº74) plus the homepage. The working tree still carries the uncommitted back-link mesh and
new-post files from the 07-02 runs; this pass re-verified all of it and found everything consistent.

## Automated checks (current state)

| Check | Result |
|---|---|
| sitemap.xml valid XML | PASS |
| feed.xml valid XML | PASS |
| sitemap loc count / feed item count | 28 / 28 (homepage + 27 posts) |
| All 27 posts present in sitemap / feed / llms.txt / index | PASS |
| Reverse check: every sitemap loc maps to a real file | PASS (no dangling) |
| JSON-LD parses on every post + index | PASS (no parse errors) |
| `<div>` open/close balance, all 55 HTML files | PASS (every file balanced) |
| Internal `.html` links resolve | PASS (no broken sibling links) |
| Local resource refs (png/svg/css/js/ico) resolve | PASS |
| Back-link graph in-degree | uniform 26 on all 27 posts |
| Back-link graph out-degree | uniform 26 on all 27 posts (no self-links) |
| Dispatch numbers unique and contiguous | PASS (Nº48–Nº74, 27 unique, no gaps, no dups) |
| Dispatch stub pages (the-*.html) target real posts | PASS (27 stubs, all redirect to live posts) |
| Placeholder/template tokens (TODO, lorem, {{}}, NaN, undefined) | none |
| Images missing alt text | none |
| Per-post visible "MIN READ" vs schema `timeRequired` | PASS (all agree) |
| Per-post canonical / og:url match filename | PASS |
| Per-post schema `dateModified` vs sitemap `<lastmod>` | PASS (0 mismatches across 27 posts) |
| robots.txt sitemap reference / CNAME | correct (blog.dugong.live) |

## Homepage counters — consistent

Hero "ISSUE Nº 74", hero date "JUL 2 · 2026", hero stat "73 dispatches published", archive
"browse all 74 dispatches", and ticker "2 NEW DISPATCHES THIS MONTH" (both marquee copies) all agree.
The hero stat "73 published" follows the site's established offset convention (published = issue
number minus 1; it read "72" when the latest issue was Nº73 in the committed HEAD, and was correctly
bumped to "73" when Nº74 shipped). The archive counter counts inclusively (= issue number). Homepage
links to all 27 posts.

## Newest post (Nº74) self-consistency

`how-to-combine-multiple-orders-from-the-same-customer-on-shopify.html` — ticker, visible meta, and
schema all agree: Nº74, JUL 2 2026, 9 MIN READ, 2,050 WORDS, `timeRequired` PT9M,
`datePublished`/`dateModified` 2026-07-02. Canonical and og:url match the filename. Out-degree 26
(links to every sibling, none to itself). Stub
`the-second-order-that-arrives-before-the-first-ships.html` redirects correctly.

## Reviewed and intentionally not changed

- The 7 em dashes in the newest post are all template-level and cross-page-consistent (title suffix,
  RSS-link title, CSS comments, two ticker date lines), matching every existing post. Authored body
  copy remains dash-free.
- The decorative `aria-hidden` hero marquee ("◆ DISPATCH Nº 74 — JUL 2, 2026") and the static
  "DISPATCH OF THE WEEK" featured card are sample/decorative styling, not live counters. Left as-is,
  consistent with prior runs.
- sitemap `lastmod` values reflect last modification (back-link wiring), not original publish date;
  each post's schema `dateModified` matches its `lastmod` exactly, so there is no inconsistency.
- The `.fuse_hidden*` and `.DS_Store` files in the working tree are OS/editor artifacts, not
  referenced by any page. Left untouched.

## Note on changes

No edits were required this pass. Consistent with prior runs, no git commit or deploy was performed;
the 07-02 working-tree changes (new post Nº74 and its back-link mesh) remain staged for the publish
pipeline to own.

## Verdict

Site is structurally sound: all XML valid, all schema parses, all links resolve both directions, all
counts agree (27 posts across sitemap/feed/llms/index), back-link graph uniform at 26/26, dispatch
numbers unique and contiguous (Nº48–Nº74), all 27 stub pages redirect to real posts, all per-post
metadata self-consistent, homepage counters consistent. No issues found; site is clean.
