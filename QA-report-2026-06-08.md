# Dugong Field Notes — QA run

**Date:** 2026-06-08 · **Scope:** blog.dugong.live (index + 5 posts + config files + icons + new RSS feed)
**Mode:** automated scheduled QA

## Bug found & fixed

**Stale dispatch count on the homepage.** The dispatch badges run sequentially and the newest
issue is **Nº 52** (`the-bad-address-that-ships-anyway`), with the lead essay at Nº 47 and the
five posts mapping Nº 48–52 in date order. But the hero stat read **"48 dispatches published"**
and the archive footer link read **"browse all 48 dispatches →"**. A reader sees a Nº 52 badge
next to a claim of 48 total — a visible contradiction. This was flagged (not changed) in the two
prior runs because the issue↔count mapping was ambiguous; this run resolves it to match the latest
issue number. Both spots now read **52**.

## Verified — no issues

- **Concurrent RSS feed addition (new this run):** a parallel process added `feed.xml`, an
  `<link rel="alternate" type="application/rss+xml" href="/feed.xml">` to all six pages, and
  changed the footer "RSS" links from placeholder `#`/`dugong.live` to `/feed.xml`. Checked it
  out: `feed.xml` is well-formed XML, the target file exists (so the new links are not broken),
  and its 6 `<item>` links all point to live URLs matching the sitemap.
- **Dates:** `dateModified` on all five posts is 2026-06-08 and every `sitemap.xml` `lastmod` is
  2026-06-08, consistent with today's RSS/feed and dispatch-count edits. `datePublished` values
  unchanged and consistent (05-28, 05-28, 06-04, 06-05, 06-06, 06-06).
- **Metadata:** self-referential canonicals match `og:url` on every page; titles correct; all six
  meta descriptions are ≤159 chars; `robots` is `index, follow …` everywhere (no stray noindex).
- **Structured data:** all six JSON-LD blocks parse; the homepage `Blog` lists all 6 posts
  (`blogPost`) with correct authors/dates (lead essay → Aanya Rao; five posts → Priya Singh),
  matching `llms.txt`. Per-post `wordCount` matches the on-page WORDS ticker (1780, 1750, 1600,
  2180, 2040) — the disagreement fixed last run is still aligned.
- **Internal links & navigation:** every local `.html` href resolves; each post's "Read next"
  graph links to all 4 siblings and none to itself; archive/anchor IDs intact.
- **Crawler config:** `robots.txt` allows search + AI crawlers and points at the sitemap; `CNAME`
  matches the canonical domain; `sitemap.xml` lists exactly the 6 live URLs.
- **Icons:** `favicon.svg`, `favicon.png`, `apple-touch-icon.png` referenced on every page (4
  icon/RSS link tags each).
- **HTML structure:** all six files parse with balanced tags (0 unclosed, 0 stray closers).

## Recommendations (not auto-changed)

- The hero banner says **"13 NEW DISPATCHES THIS MONTH"** but only ~4 dispatches carry a
  June 2026 date. Reads as decorative copy not tied to a countable source; left as-is.
- Visible WORDS tickers still run ~10–25% above the measured body text (e.g. wismo claims 2,040;
  rendered body ≈1,785). Internally consistent with the schema, so harmless for SEO, but worth a
  recount if literal accuracy matters.
- `robots.txt`/`sitemap.xml` don't advertise the new `feed.xml`. Optional: feeds are normally
  discovered via the `<link rel="alternate">` tags (already present), so this is not required.

## Note

A separate process was actively writing to the working tree during this run (RSS feed, date
bumps); the git index was locked, so no commit or deploy was performed. Changes from both this QA
run and the RSS work are staged in the working tree only.
