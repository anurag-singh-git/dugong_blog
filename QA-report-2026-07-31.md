# QA report, 2026-07-31

**Scope:** Full-site QA sweep, the first since 07-30. In between, the 07-30 publish run landed
Dispatch Nº 95 (*The wholesale application that sits all weekend* /
`how-to-automatically-approve-wholesale-customers-on-shopify.html`) plus its pain-point stub,
and the 07-31 SEO/GEO pass edited 50 files: the Nº 95 READ NEXT mesh card prepended to all 47
sibling posts, the `#dispatch-47` hub-graph back-fill on index.html, and sitemap lastmods. The
site now carries **97 HTML files**: 49 rendered pages (48 posts plus index.html) and 48 redirect
stubs, alongside sitemap.xml, feed.xml, llms.txt, robots.txt.

Every check was re-derived from scratch rather than trusting the 07-30 or 07-31 reports.

**Result: zero defects found. Nothing needed fixing.** This is the first fully clean pass on
record, beating 07-30's single-defect pass. Both recent bulk edits (the 47-file mesh insert and
the hub-graph completion) verified correct in every particular.

## The 07-31 SEO edits, independently verified

The mesh insert was the risky change: 47 files edited by script in one pass. All of it checks out.

| Check | Result |
|---|---|
| Nº 95 card present in READ NEXT on siblings | 47 / 47 |
| card sits first in the grid (newest-first convention) | 47 / 47 |
| card markup byte-identical across all 47 siblings | yes, exactly 1 distinct body |
| card dek == Nº 95's meta description verbatim | yes |
| card read-time chip (9 min) == Nº 95 `timeRequired` PT9M | yes |
| tag balance on all 47 edited siblings | clean |
| hub graph: 49 / 49 entries carry all fields incl. `dateModified` + `wordCount` | PASS (the `#dispatch-47` back-fill verified) |
| hub Nº 95 entry: 13 fields, author by `@id` ref to `#priya-singh`, wordCount 2334 | PASS |
| sitemap lastmod 2026-07-31 only on files actually edited; Nº 95 keeps 07-30 | PASS |

## Structural (all pass)

| Check | Result |
|---|---|
| tag balance / nesting / stray tags, all 97 files (custom scanner) | 0 problems |
| JSON-LD parses on every rendered page | 49 / 49 |
| titles present and unique | 49 / 49 |
| meta descriptions present, unique, 160 chars or under | 49 / 49 |
| canonical matches filename, and canonical == og:url | 49 / 49 |
| exactly one h1 per page; no skipped heading levels | 49 / 49 |
| og: and twitter: meta on every rendered page | 49 / 49 |
| `<html lang>`, charset, viewport | 49 / 49 |
| CSS brace balance | PASS |
| placeholders (TODO, TBD, `{{ }}`, NaN, Lorem ipsum), empty elements, replacement chars | 0 |
| `<img>` without alt | 0 |

## Links and surfaces (all pass)

| Check | Result |
|---|---|
| internal links resolve (relative, root-relative, absolute, refresh targets) | 0 broken |
| redirect stubs: noindex + canonical + live refresh target | 48 / 48 |
| posts on sitemap / feed / llms.txt / index card grid | 48 / 48 on every surface |
| sitemap: 49 locs, 0 dupes, 0 ghosts, lastmod ≥ datePublished | PASS |
| feed: 49 items, 0 dup guids, pubDate == datePublished, lastBuildDate = Jul 30 (newest item) | PASS |
| hub `Blog` graph vs each post: headline / datePublished / dateModified / wordCount | 0 mismatches |
| dispatch numbers 48 through 95: unique, gap-free, one per post | PASS (48 posts) |
| cross-page read-time chips vs target `timeRequired`, every card site-wide | 0 mismatches |
| own-page read-time chip vs own `timeRequired` | 49 / 49 |
| FAQ JSON-LD questions vs rendered `.faq-q`, all FAQPage posts | in sync |
| `article:modified_time` vs `dateModified` | 0 mismatches |

## Homepage counters (all correct)

Verified independently against `datePublished` across all 48 posts (2026-05 ×1, 2026-06 ×24,
2026-07 ×23):

- `.hero-issue` = ISSUE Nº 95, matches newest post (Nº 95, Jul 30) ✓
- `.hero-date` = JUL 30 · 2026 ✓
- dispatches `.stat-num` = 95 ✓
- ticker `◆ 23 NEW DISPATCHES THIS MONTH`, both marquee spans ✓ (July truly has 23)
- exactly one `card-tall` in the grid, and it is Nº 95 ✓

The publish-step counter bug that ran 07-25 through 07-27 stayed dead for a second consecutive
publish.

## Mobile and design (all pass)

| Check | Result |
|---|---|
| `width=device-width` viewport on all rendered pages | 49 / 49 |
| max-width media queries present | 49 / 49 |
| `overflow-x` guard on html/body | 49 / 49 |
| `<pre>` pre-wrap rule at 560px | 49 / 49 |
| CSS template drift | none: three groups, byte-identical within each: 18,874 ×44 standard posts (Nº 95 joined cleanly), 32,935 ×4 hub/pillar, 32,924 ×1 index |
| playbook `<pre>` line lengths | 921 lines, max 71, zero over 72 |
| font sizes below the 0.64rem chip floor | 0 |
| long unbreakable prose tokens over 48 chars | 0 |

Headless-browser render at 360px remains unavailable in this sandbox; mobile assessed by CSS
analysis, as in all prior passes.

## Content and readability

| Check | Result |
|---|---|
| British spelling, 60+ patterns | 0 hits (07-30's `neighbours` fix holds; no new drift) |
| common-typo and brand-typo scan (~50 patterns) | 0 |
| doubled-word scan | 15 raw hits, all title/dek element boundaries ("...on Shopify" / "Shopify will..."), 0 real |
| double spaces inside paragraphs | 0 |
| em/en dashes in authored prose | 0 (ticker chrome dashes are the standing template convention) |
| paragraph-level read of Nº 95 | clean, see below |

**Read of Nº 95.** First QA content read of this post (published 07-30, after the last QA pass).
No typos, no grammar defects, no dash drift. Internal consistency verified: the ceramics-brand
example sums correctly (19 + 4 + 2 + 1 + 4 = 30 applications); the playbook has seven steps and
the prose discusses steps two through five in order. The Shopify claims match what the 07-30
publish run researched and are correctly hedged: Plus-only B2B company account requests via
Shopify Forms (creates company + location + customer, vets nothing), the "Restrict access to B2B
customers only" login screen with no registration path, the open-login six-digit-code hole, the
locked-down new customer accounts (no Liquid, no app embeds), and the non-Plus three-app
workaround. The two community-thread titles are quoted as titles, consistent with the run notes.
Cross-references (high-risk flag, repeat customer, invoice, volume discounts, minimum order,
automations pillar) all resolve and are apt. The 3-question FAQ renders and matches its JSON-LD
exactly. wordCount 2,334 consistent across post JSON-LD, hub graph, and body (prose-token count
methodology, matching prior dispatches).

## Checked and left as-is (carried conventions)

- Pinned inline lead essay at `index.html#post`: intentional; explains 49 index/feed entries vs
  48 post files. The feed's homepage-linking item is this essay, not a ghost.
- Redirect stubs carry no viewport meta; noindex instant redirects.
- Pinned essay's visible 14-min chip vs 893 `wordCount`: flagged 07-31 for the owner, unchanged.
- Read-time words-per-minute varies by vintage; every surface internally consistent.
- feed.xml descriptions longer than og:description; by design.
- Template-chrome `&mdash;` and CSS-comment dashes: outside house-style enforcement.
- Stray `.fuse_hidden*` files in repo root are mount artifacts, untouched.

## Open items

- **Working tree is undeployed.** Everything since the 07-28 deploy state (07-28 publish and SEO
  pass, 07-30 QA fix and publish, 07-31 SEO pass) remains uncommitted. Nothing changes on
  blog.dugong.live until someone commits and pushes. Left uncommitted per convention: deploys are
  the owner's call.
- SEO pass recommendations carried forward: add the sibling-mesh insert to the publish step
  (the one surface the publish template still misses), FAQ depth beyond 3 per post, author pages
  for the Person entities.
- Uncovered topic candidates carried forward: COD/RTO verification, draft-order invoice
  follow-ups, cross-product BOGO, loyalty-points automation, gift-card automation, multi-store
  sync.
