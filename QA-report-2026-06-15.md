# Dugong Field Notes — QA run (2026-06-15)

**Date:** 2026-06-15 · **Scope:** blog.dugong.live (index + 12 posts + feed/sitemap/llms/robots + icons)
**Mode:** automated scheduled QA (autonomous; no user present)

## Result: one defect found and fixed.

Today's daily blog run published Dispatch Nº 59 ("The reorder you always make too late") and
wired it into every discovery surface, but shipped with the usual incomplete internal link
graph: only the two most related siblings (restock and overselling) back-linked the new post.
The other nine older posts did not. This QA pass closed that gap. Every other surface was
already correct and was re-swept in the settled state.

## Defect fixed

**Incomplete back-link graph for Nº 59.** Nine older posts did not link to the new reorder
post: the-abandoned-cart, the-bad-address, the-chargeback, the-high-risk-flag, the-order-change,
the-return, the-shopify-automations, the-sold-out, and the-wismo. Inserted the standard reorder
Read-next card (markup and copy verbatim from the card restock and overselling already use) as
the first card in each of the nine grids. After the edits every one of the 12 posts links to
exactly 11 unique siblings, the Nº 59 link appears exactly once per older post, and there are no
self-links. The card text is dash-free, consistent with the no-dash style preference. No article
body text, titles, dates, or word counts were touched, and `dateModified` was deliberately not
bumped on the edited posts (consistent with prior runs' house behavior for link-card additions).

## Verified — no other issues

- **HTML structure.** `<div>` open/close balanced on all 13 HTML files (index 146/146; the three
  inventory-cluster posts and the nine edited posts at 61/61, the older unedited group already at
  58 before this run, now all 61 after the card insertions). No duplicate `id` attributes anywhere.
  Title, `lang`, charset, and viewport present on every page.
- **Structured data.** Every `application/ld+json` block parses as valid JSON (one block per page)
  after the edits.
- **Dispatch numbering.** The 12 post files carry unique numbers Nº 48 through Nº 59 in date
  order, no duplicates. Homepage hero reads ISSUE Nº 59, the stat reads 59 dispatches published,
  and the archive link reads browse all 59 dispatches — all three agree with the highest number.
- **Internal link graph.** Every post now links to all 11 sibling posts (12 unique local targets
  including the homepage), no self-links, and zero broken local `.html` links across all pages.
- **Canonical / Open Graph / Twitter.** `rel="canonical"` equals `og:url` on all 13 pages (one
  each). `og:image:alt`, `twitter:image:alt`, and `twitter:card` each present exactly once per page.
- **Meta descriptions.** All within SERP limits: index 100 chars, all 12 posts in the 146–160
  range. The new Nº 59 post is 154 (shipped within limit, no trim needed).
- **Freshness metadata.** New post carries one `article:published_time` and one
  `article:modified_time`; OG dates equal the schema `datePublished` / `dateModified` (all
  2026-06-15). Sitemap `<lastmod>` for the reorder post is 2026-06-15 and homepage is 2026-06-15.
- **Feed / sitemap / llms.txt.** Both XML files are well-formed. Sitemap lists 13 URLs (homepage
  + 12 posts), feed lists 13 items (guid count 13), and llms.txt references all 12 posts. Feed
  `lastBuildDate` is Mon, 15 Jun 2026 and the newest item is the reorder post.
- **New post self-consistency.** Nº 59 ticker, visible meta, and schema all agree: Nº 59,
  JUN 15 2026, 9 MIN READ / `PT9M`, 1,860 words visible / `wordCount` 1860, dates 2026-06-15,
  author Priya Singh.
- **Crawler config / assets.** `robots.txt` allows the major search and AI crawlers and points at
  the sitemap; `CNAME` is `blog.dugong.live`; every referenced icon and asset
  (`favicon.svg/.png`, `apple-touch-icon.png`, `og-image.png`) exists on disk.

## Recommendations (not auto-changed, consistent with prior runs)

- The recurring pattern persists: each new dispatch ships with only the two most-related
  back-links and gets the full back-link graph completed the next day. Folding the full back-link
  insertion into the blog-run step itself would remove this daily cleanup. (Today the meta length
  shipped in range, so only the back-link half of the usual cleanup was needed.)
- Homepage hero still reads "13 NEW DISPATCHES THIS MONTH" and the static "DISPATCH OF THE WEEK"
  card still references Nº 47 — decorative copy not tied to a countable source. Editorial call;
  left as-is.
- Older posts (Nº 48–55, plus wismo) still carry em/en dashes in their authored bodies, predating
  the no-dash style preference. Rewriting that authored prose risks altering meaning, so it is an
  editorial change, not a QA fix; left as-is.
- `HowTo` / step `ItemList` schema for the ordered "AI playbook" sequences remains the largest
  un-captured GEO opportunity.

## Note

The nine back-link card insertions are staged in the working tree only. Consistent with prior
runs, nothing was committed or deployed.
