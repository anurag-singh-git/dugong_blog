# Dugong Field Notes — QA run

**Date:** 2026-06-10 · **Scope:** blog.dugong.live (index + 7 posts + feed/sitemap/llms/robots + icons)
**Mode:** automated scheduled QA (autonomous; no user present)

## Result: clean bill of health, no fixes required

Ran a full sweep across all eight HTML pages plus the config and feed files. Every check passed.
The recurring dispatch-count drift that the 06-08 and earlier runs had to correct is now internally
consistent, so there was nothing to change this run. No files were modified.

## Verified — no issues

- **Dispatch numbering and counts now agree.** The newest issue is **Nº 54**
  (`the-return-that-wont-approve-itself`, Jun 9). The hero stat reads **54 dispatches published**,
  the archive footer reads **browse all 54 dispatches**, and the hero badge reads **ISSUE Nº 54**.
  All three match the highest dispatch number. The seven posts map cleanly to Nº 48–54 in date
  order, with the inline lead essay at Nº 47 (May 28). No contradiction between a visible badge and
  a total count, which was the defect class fixed in prior runs.
- **Meta descriptions:** all eight are within the SERP limit (100–159 chars). Titles present and
  unique on every page.
- **Canonical / Open Graph:** `rel="canonical"` equals `og:url` on all eight pages; exactly one of
  each per page. `og:type` is `website` on the index and `article` on all seven posts;
  `twitter:card` is `summary_large_image` everywhere.
- **Freshness metadata (added by today's SEO-GEO run) is well-formed and consistent.** Each post
  carries exactly one `article:published_time` and one `article:modified_time`; on every post the
  OG published time equals the schema `datePublished` and the OG modified time equals the schema
  `dateModified` (2026-06-09). `og:image:alt` and `twitter:image:alt` are present on all 8 pages
  (one each, no duplicates). No duplicated or malformed head tags were introduced.
- **Internal links:** every local `.html` href across all eight pages resolves to an existing file
  (0 broken). Each post's "Read next" block links to all six siblings and none to itself. All seven
  posts are linked from the homepage.
- **Structured data:** every JSON-LD block parses as valid JSON. The homepage `Blog` node lists 8
  `blogPost` entries = the 7 post files + the inline lead essay ("The death of the drag-and-drop
  builder", Aanya Rao); the other seven carry Priya Singh and dates matching their files.
  Organization + WebSite + Blog `@graph` intact on the index.
- **Feed / sitemap / llms.txt:** all three list exactly the same 7 post URLs (feed and sitemap each
  carry 8 entries = 7 posts + homepage/lead essay). `feed.xml` and `sitemap.xml` are well-formed
  XML. Feed item titles and links match the post files; the lead-essay item links to the homepage
  root with a `#dispatch-47` guid, which is correct since that essay lives on the index.
- **Dates:** `dateModified` is 2026-06-09 on all seven posts; every `sitemap.xml` `lastmod` and the
  feed `lastBuildDate` are also 2026-06-09. Today's run added metadata only (no article content
  changed), so these freshness dates were correctly left at 06-09 rather than inflated to 06-10.
- **Crawler config:** `robots.txt` allows search and AI crawlers and points at the sitemap;
  `CNAME` is `blog.dugong.live`, matching every canonical. Icons (`favicon.svg`, `favicon.png`,
  `apple-touch-icon.png`) and `og-image.png` are referenced on every page and the files exist.
- **HTML structure:** `<div>` open/close balanced on every file (index 131/131; the five "41"
  posts 41/41; order-change and returns 38/38). lxml recovers all eight pages with zero parse
  errors. No duplicate `id` attributes. `lang="en"`, charset, and viewport present on every page.
  No `lorem`/`TODO`/`example.com` placeholders; the only `href="#"` are the JS-routed SPA nav and
  decorative topic tags (no broken destinations), and the only "placeholder" strings are a CSS
  `::placeholder` rule and a legitimate email-input placeholder.

## Recommendations (not auto-changed, consistent with prior runs)

- Homepage hero still reads **"13 NEW DISPATCHES THIS MONTH"** — decorative copy not tied to a
  countable source (roughly 6 posts carry a June 2026 date). Editorial call; left as-is.
- Visible WORDS tickers run somewhat above the measured body text but match each page's JSON-LD
  `wordCount`, so they are internally consistent and harmless for SEO. Left as-is.

## Note

No bugs found, so no files were changed and no git commit or deploy was performed, consistent with
prior runs.
