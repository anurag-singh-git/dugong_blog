# QA report, 2026-08-01

**Scope:** Full-site QA sweep, the first since 07-31. Two runs landed in between: the 07-31
publish run shipped Dispatch Nº 96 (*The pickup order that should have shipped* /
`how-to-change-a-shopify-order-from-local-pickup-to-shipping.html`) plus its pain-point stub,
with the sibling mesh executed at publish time for the first time; and the 08-01 SEO/GEO pass
edited 50 files (the author `@id` consolidation on all 49 posts, plus sitemap lastmods). The
site now carries **99 HTML files**: 50 rendered pages (49 posts plus index.html) and 49
redirect stubs, alongside sitemap.xml, feed.xml, llms.txt, robots.txt.

Every check was re-derived from scratch rather than trusting the 07-31 QA report or the 08-01
SEO report.

**Result: zero defects found. Nothing needed fixing.** Second consecutive fully clean pass.
Both intervening bulk edits (the 48-file mesh insert at publish, and today's 49-file author
`@id` edit) verified correct in every particular.

## The 08-01 SEO edit, independently verified

The author `@id` consolidation was today's risky change: 49 files edited by script this
morning. All of it checks out.

| Check | Result |
|---|---|
| `BlogPosting.author` carries `@id` `https://blog.dugong.live/#priya-singh` | 49 / 49 |
| author blocks byte-identical across posts | yes, exactly 1 distinct block |
| inline `name` / `jobTitle` / `worksFor` retained alongside the `@id` | yes |
| site-level `#priya-singh` Person node (with `knowsAbout`) resolves in index graph | PASS |
| JSON-LD re-parses on every rendered page after the edit | 50 / 50 |
| sitemap lastmod 2026-08-01 on exactly the 49 edited posts; index keeps 07-31 | PASS |
| `dateModified` not bumped (metadata-only change, standing ruling) | consistent, 0 drift vs `article:modified_time` |

## The 07-31 publish (Nº 96), independently verified

First publish run to execute the sibling mesh at publish time; verified as if it were an SEO
bulk edit.

| Check | Result |
|---|---|
| Nº 96 card present in READ NEXT on siblings | 48 / 48 |
| card sits first in the grid (newest-first convention) | 48 / 48 |
| card markup byte-identical across all 48 siblings | yes, exactly 1 distinct body |
| card read-time chip (9 min) == Nº 96 `timeRequired` PT9M | yes |
| Nº 96 on sitemap / feed / llms.txt / index grid | all four surfaces |
| llms.txt entry inserted directly after the lead essay (newest-first) | yes |
| stub `the-pickup-order-that-should-have-shipped.html`: noindex + canonical + live target | PASS |

## Structural (all pass)

| Check | Result |
|---|---|
| tag balance / nesting / stray tags, all 99 files (custom scanner) | 0 problems |
| JSON-LD parses on every rendered page | 50 / 50 |
| titles present and unique | 50 / 50 |
| meta descriptions present, unique, 160 chars or under | 50 / 50 |
| canonical matches filename, and canonical == og:url | 50 / 50 |
| exactly one h1 per page; no skipped heading levels | 50 / 50 |
| og: and twitter: meta on every rendered page | 50 / 50 |
| `<html lang>`, charset, viewport | 50 / 50 |
| CSS brace balance | PASS |
| placeholders (TODO, TBD, `{{ }}`, NaN, Lorem ipsum), replacement chars | 0 |
| `<img>` without alt | 0 |

## Links and surfaces (all pass)

| Check | Result |
|---|---|
| internal links resolve (relative, root-relative, absolute, refresh targets) | 0 broken |
| redirect stubs: noindex + canonical + live refresh target | 49 / 49 |
| posts on sitemap / feed / llms.txt / index card grid | 49 / 49 on every surface |
| sitemap: 50 locs, 0 dupes, 0 ghosts, lastmod ≥ datePublished | PASS |
| feed: 50 items, 0 dup guids, pubDate == datePublished, lastBuildDate = Jul 31 (newest item) | PASS |
| hub `Blog` graph vs each post: headline / datePublished / dateModified / wordCount | 0 mismatches, 50 / 50 entries fully populated |
| dispatch numbers 48 through 96: unique, gap-free, one per post | PASS (49 posts) |
| cross-page read-time chips vs target `timeRequired`, every card site-wide | 0 mismatches |
| own-page read-time chip vs own `timeRequired` | 50 / 50 |
| FAQ JSON-LD questions vs rendered `.faq-q`, all FAQPage posts | in sync |
| `article:modified_time` vs `dateModified` | 0 mismatches |

## Homepage counters (all correct)

Verified independently against `datePublished` across all 49 posts (2026-05 ×1, 2026-06 ×24,
2026-07 ×24):

- `.hero-issue` = ISSUE Nº 96, matches newest post (Nº 96, Jul 31) ✓
- `.hero-date` = JUL 31 · 2026 ✓
- dispatches `.stat-num` = 96 ✓
- ticker `◆ 24 NEW DISPATCHES THIS MONTH`, both marquee spans ✓ (July truly has 24)
- exactly one `card-tall` in the grid, and it is Nº 96 ✓ (07-30 card correctly demoted)

## Mobile and design (all pass)

| Check | Result |
|---|---|
| `width=device-width` viewport on all rendered pages | 50 / 50 |
| max-width media queries present | 50 / 50 |
| `overflow-x` guard on html/body | 50 / 50 |
| `<pre>` pre-wrap rule at 560px | 50 / 50 |
| CSS template drift | none: three groups, byte-identical within each: 18,874 ×45 standard posts (Nº 96 joined cleanly), 32,935 ×4 hub/pillar, 32,924 ×1 index |
| playbook `<pre>` line lengths | 951 lines, max 71, zero over 72 |
| font sizes below the 0.64rem chip floor | 0 |
| long unbreakable prose tokens over 48 chars | 0 |

Headless-browser render at 360px remains unavailable in this sandbox; mobile assessed by CSS
analysis, as in all prior passes.

## Content and readability

| Check | Result |
|---|---|
| British spelling, 60+ patterns | 0 hits |
| common-typo and brand-typo scan (~50 patterns) | 0 |
| doubled-word scan | 1 raw hit, a clause boundary in the repeat-customer post ("what a customer is / is not"), grammatical, 0 real |
| double spaces inside paragraphs | 0 |
| em/en dashes in authored prose | 0 (ticker chrome dashes remain the standing template convention) |
| paragraph-level read of Nº 96 | clean, see below |

**Read of Nº 96.** First QA content read of this post (published 07-31, after the last QA
pass). No typos, no grammar defects, no dash drift. Internal consistency verified: the Austin
home-goods example sums correctly (five flagged at creation plus one reverse case equals the
six weekly wrong-method orders; four ship answers, one pickup answer, one shipping refund);
the playbook has seven steps and the prose discusses steps one through four in order, then
the tag (step six) and the weekly count (step seven). The Shopify claims match what the 07-31
publish run researched and are correctly hedged: checkout preselects the cheapest delivery
option with no setting to change the default, reorder the list, or make pickup opt-in; order
editing does not touch the delivery method and pickup/local-delivery orders often refuse
editing entirely; the supported conversion path is cancel, refund, rebuild as a draft order,
re-invoice; the label-anyway half workaround leaves an armed pickup notification and an eaten
shipping fee. The three community-thread references are quoted as titles, consistent with the
run notes. The Marietta-to-Denver "1,400 miles" figure is a plausible driving distance.
Cross-references (order editing, high-risk flag, duplicate order, WISMO, order tagging,
address validation, automations pillar, order-change race, delayed order) all resolve and are
apt. The 3-question FAQ renders and matches its JSON-LD exactly. Read-time chip (9 MIN) agrees
with `timeRequired` PT9M on the post and on every cross-page card.

## Checked and left as-is (carried conventions)

- Pinned inline lead essay at `index.html#post`: intentional; explains 50 index/feed entries vs
  49 post files. The feed's homepage-linking item is this essay, not a ghost.
- Redirect stubs carry no viewport meta; noindex instant redirects.
- Pinned essay's visible 14-min chip vs 893 `wordCount`: flagged 07-31 for the owner, unchanged.
- Read-time words-per-minute varies by vintage; every surface internally consistent.
- feed.xml descriptions longer than og:description; by design.
- Template-chrome `&mdash;` and CSS-comment dashes: outside house-style enforcement.
- Stray `.fuse_hidden*` files in repo root are mount artifacts, untouched.

## Open items

- **Working tree is undeployed.** Everything since the 07-28 deploy state (07-28 publish and
  SEO pass, 07-30 QA fix and publish, 07-31 SEO pass and publish, 08-01 SEO pass) remains
  uncommitted. Nothing changes on blog.dugong.live until someone commits and pushes. Left
  uncommitted per convention: deploys are the owner's call.
- SEO pass recommendations carried forward: FAQ depth beyond 3 per post (highest-value GEO
  surface, needs editorial judgement), author pages for the Person entities (schema half
  closed by the 08-01 `@id` consolidation; human-readable half remains).
- Uncovered topic candidates carried forward: COD/RTO verification, draft-order invoice
  follow-ups, cross-product BOGO, loyalty-points automation, gift-card automation, multi-store
  sync.
